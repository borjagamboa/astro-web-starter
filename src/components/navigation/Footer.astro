---
import { site } from '../../data/site';
import {
  footerNavigation,
  legalNavigation,
  type FooterNavigationGroup,
  type NavigationItem,
} from '../../data/navigation';
import Logo from './Logo.astro';

interface Props {
  groups?: readonly FooterNavigationGroup[];
  legalLinks?: readonly NavigationItem[];
  showDescription?: boolean;
}

const {
  groups = footerNavigation,
  legalLinks = legalNavigation,
  showDescription = true,
} = Astro.props;

const hasContact = Boolean(site.contact?.email || site.contact?.phone || site.location);
const hasSocial = site.socialLinks.length > 0;
const currentYear = new Date().getFullYear();
const phoneHref = site.contact?.phone?.replace(/[^+\d]/g, '');
---

<footer class="site-footer">
  <div class="site-footer__inner">
    <div class="site-footer__brand">
      <Logo variant="inverse" />
      {showDescription && site.description && <p>{site.description}</p>}
    </div>

    {groups.map((group) => group.items.length > 0 && (
      <nav class="footer-group" aria-label={group.label}>
        <h2>{group.label}</h2>
        <ul>
          {group.items.map((item) => <li><a href={item.href}>{item.label}</a></li>)}
        </ul>
      </nav>
    ))}

    {hasContact && (
      <section class="footer-group" aria-labelledby="footer-contact-title">
        <h2 id="footer-contact-title">Contacto</h2>
        <ul>
          {site.contact?.email && <li><a href={`mailto:${site.contact.email}`}>{site.contact.email}</a></li>}
          {site.contact?.phone && <li><a href={`tel:${phoneHref}`}>{site.contact.phone}</a></li>}
          {site.location && <li>{site.location}</li>}
        </ul>
      </section>
    )}

    {hasSocial && (
      <nav class="footer-group" aria-label="Redes sociales">
        <h2>Social</h2>
        <ul>
          {site.socialLinks.map((link) => <li><a href={link.href}>{link.label}</a></li>)}
        </ul>
      </nav>
    )}
  </div>

  <div class="site-footer__bottom">
    <p>© {currentYear} {site.name}</p>
    {legalLinks.length > 0 && (
      <nav aria-label="Información legal">
        <ul>
          {legalLinks.map((item) => <li><a href={item.href}>{item.label}</a></li>)}
        </ul>
      </nav>
    )}
  </div>
</footer>

<style>
  .site-footer {
    background: var(--color-footer-background);
    color: var(--color-text-on-inverse);
  }

  .site-footer__inner,
  .site-footer__bottom {
    width: min(100% - (var(--gutter) * 2), var(--container-wide));
    margin-inline: auto;
  }

  .site-footer__inner {
    display: grid;
    gap: var(--space-7);
    padding-block: var(--space-section) var(--space-7);
  }

  .site-footer__brand {
    display: grid;
    align-content: start;
    gap: var(--space-4);
  }

  .site-footer__brand p {
    margin: 0;
    color: var(--color-text-on-inverse);
  }

  .footer-group h2 {
    margin: 0 0 var(--space-4);
    color: var(--color-heading-on-inverse);
    font-size: var(--text-sm);
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .footer-group ul,
  .site-footer__bottom ul {
    display: grid;
    gap: var(--space-2);
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .site-footer a {
    color: var(--color-text-on-inverse);
  }

  .site-footer a:hover {
    color: var(--color-heading-on-inverse);
  }

  .site-footer__bottom {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: var(--space-4);
    border-top: 1px solid rgb(255 255 255 / 18%);
    padding-block: var(--space-5);
    font-size: var(--text-sm);
  }

  .site-footer__bottom p {
    margin: 0;
  }

  .site-footer__bottom ul {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-4);
  }

  @media (min-width: 48rem) {
    .site-footer__inner {
      grid-template-columns: minmax(16rem, 2fr) repeat(3, minmax(8rem, 1fr));
    }
  }
</style>
