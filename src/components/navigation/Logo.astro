---
import { site, type SiteConfig } from '../../data/site';

interface Props {
  href?: string;
  variant?: 'default' | 'inverse';
  siteConfig?: SiteConfig;
  class?: string;
}

const { variant = 'default', siteConfig = site, class: className } = Astro.props;
const href = Astro.props.href ?? siteConfig.homeHref ?? '/';
const configuredLogo = variant === 'inverse' ? (siteConfig.brand.logoInverse ?? siteConfig.brand.logo) : siteConfig.brand.logo;
---

<a
  class:list={['logo', `logo--${variant}`, { 'logo--image': configuredLogo }, className]}
  href={href}
  aria-label={`${siteConfig.name}, inicio`}
>
  {configuredLogo ? (
    <img
      src={configuredLogo.src}
      width={configuredLogo.width}
      height={configuredLogo.height}
      alt=""
      aria-hidden="true"
      loading="eager"
      decoding="async"
    />
  ) : (
    <span class="logo__mark" aria-hidden="true">{(siteConfig.shortName ?? siteConfig.name).slice(0, 1)}</span>
    <span class="logo__name">{siteConfig.shortName ?? siteConfig.name}</span>
  )}
</a>

<style>
  .logo {
    display: inline-flex;
    min-width: 0;
    align-items: center;
    gap: var(--space-3);
    color: var(--color-heading);
    font-weight: 750;
    line-height: 1;
    text-decoration: none;
  }

  .logo:hover {
    color: var(--color-heading);
  }

  .logo__mark {
    display: grid;
    width: 2.4rem;
    aspect-ratio: 1;
    flex: 0 0 auto;
    place-items: center;
    border-radius: var(--radius-logo);
    background: var(--color-action);
    color: var(--color-on-action);
    font-size: var(--text-md);
  }

  .logo__name {
    overflow: hidden;
    max-width: 15rem;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .logo--inverse {
    color: var(--color-heading-on-inverse);
  }

  .logo--inverse:hover {
    color: var(--color-heading-on-inverse);
  }

  .logo--inverse .logo__mark {
    background: var(--color-accent);
    color: var(--color-heading);
  }

  img {
    width: auto;
    max-width: min(12rem, 45vw);
    max-height: 2.75rem;
    object-fit: contain;
  }
</style>
